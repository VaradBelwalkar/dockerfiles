FROM archlinux

RUN pacman -Sy --noconfirm && pacman -S --noconfirm openssh rsync sudo && echo 'root:password' | chpasswd && echo "PasswordAuthentication yes" >> /etc/ssh/sshd_config && touch /root/.ssh/authorized_keys  && chsh -s /bin/bash

CMD ["/usr/bin/sshd","-D"]	
